# RBMS TCP 模拟器 — 测试规格说明

> **文档定位**：将 `需求文档.md` 中每条需求展开为可执行的测试用例，作为开发（含 AI 生成代码）的验收标准。  
> **约定**：`[✓]` = 已有测试覆盖，`[✗]` = 尚无测试，`[—]` = 无需自动化测试（人工验收）。  

---

## 1. 通信层测试（FR-COM）

### TC-COM-01：HMI 通道主动连接
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-01 |
| 级别 | 集成测试 |
| 前置 | 启动一个 TCP Server 监听 `127.0.0.1:5001`（模拟 HMI） |
| 步骤 | 启动模拟器（`rbms-sim`） |
| 预期 | Server 侧收到 connect；模拟器日志输出 `已连接` |
| 现有测试 | [✓] `test_hmi_client_connects_to_server` |
| 备注 | 需 mock socket 或启动临时 Server |

### TC-COM-02：HMI 断线自动重连
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-02 |
| 级别 | 集成测试 |
| 前置 | 模拟器已连接 HMI |
| 步骤 | ① 断开 HMI Server 侧连接 ② 等待 `reconnect_interval_s`（默认 5s） |
| 预期 | 模拟器日志输出 `重连` 或类似提示；再次完成 TCP 连接 |
| 现有测试 | [✓] `test_hmi_client_reconnects_after_disconnect` |
| 备注 | 验证重连间隔可配置 |

### TC-COM-03：BBMS 通道监听
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-03 |
| 级别 | 集成测试 |
| 前置 | 无 |
| 步骤 | 启动模拟器后执行 `netstat -tlnp` |
| 预期 | 默认 `0.0.0.0:5002` 处于 LISTEN 状态 |
| 现有测试 | [✓] `test_bbms_server_sends_suminfo_to_connected_client`（隐式验证） |
| 备注 | 可改用 Python `socket.connect` 验证 |

### TC-COM-04：BBMS 单连接 + 踢旧
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-04 |
| 级别 | 集成测试 |
| 前置 | 模拟器已启动 |
| 步骤 | ① BBMS-A 连接 → 验证可收发 ② BBMS-B 连接 → 验证 BBMS-A 被断开 |
| 预期 | 第二连接成功后，第一连接 recv 返回空或异常 |
| 现有测试 | [✓] `test_bbms_server_kicks_old_connection` |

### TC-COM-05：双通道并行
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-05 |
| 级别 | 集成测试 |
| 前置 | HMI Server + BBMS Client 均已连接 |
| 步骤 | 同时从两通道发送数据 |
| 预期 | 两通道各自收发正常，互不阻塞 |
| 现有测试 | [✗] |

### TC-COM-06：帧结构合规
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-06, FR-COM-07, FR-COM-09 |
| 级别 | 单元测试 |
| 前置 | 构建任意 Tx 帧 |
| 步骤 | 调用 `build_frame()` 并验证返回值 |
| 预期 | ① `frame[0]` = `0xA5` ② `(frame[1] & 0x1F)` = `2`（version） ③ `frame[3:5]` = CRC16(body) 正确 ④ `frame[5+8]` 起为 payload ⑤ `transportType` = `0x01` |
| 现有测试 | [✓] `test_build_frame_5b_link_msg_suminfo`, `test_build_frame_roundtrip` |

### TC-COM-07：CRC16 校验
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-01 |
| 级别 | 单元测试 |
| 前置 | 任意 body 数据 |
| 步骤 | ① `crc16_modbus(data)` → crc1 ② 修改 data 中任意字节 → `crc16_modbus(data')` → crc2 |
| 预期 | crc1 ≠ crc2（检测篡改） |
| 现有测试 | [✓] `test_crc16_modbus_self_consistency` |

### TC-COM-08：多字节小端编码
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-COM-08 |
| 级别 | 单元测试 |
| 前置 | `_pack_version_len(2, 318)`（body 长 318） |
| 步骤 | 检查 `word = ((318 & 0x7FF) << 5) \| 2` 以小端存入 |
| 预期 | struct.unpack('<H', data) == word |
| 现有测试 | [✗] |

---

## 2. HMI 通道测试（FR-HMI）

### TC-HMI-01：Tx 目标地址
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-HMI-01, FR-HMI-03 |
| 级别 | 单元测试 |
| 前置 | `build_periodic_tx_frames(tx_dest=DEV_HMI_BBMS_A)` |
| 步骤 | 解析任意帧的 body dest/destSub |
| 预期 | `dest=0x01, destSub=0x00`, `src=0x04, srcSub=1` |
| 现有测试 | [✓] `test_periodic_tx_dest_is_hmi_bbms_a` |

### TC-HMI-02：Rx 来源不限制
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-HMI-02 |
| 级别 | 单元测试 |
| 前置 | 构造不同 src 的 CtlWord 帧 |
| 步骤 | 调用 `dispatch()` |
| 预期 | 全部被处理，不因 src 不同而拒绝 |
| 现有测试 | [✗] |

---

## 3. BBMS 通道测试（FR-BBMS）

### TC-BBMS-01：Tx 目标地址
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-BBMS-01, FR-BBMS-03 |
| 级别 | 单元测试 |
| 前置 | `build_periodic_tx_frames(tx_dest=DEV_BBMS_M)` |
| 步骤 | 解析任意帧的 body dest/destSub |
| 预期 | `dest=0x03, destSub=0x01`, `src=0x04, srcSub=1` |
| 现有测试 | [✓] `test_periodic_tx_dest_bbms_channel` |

### TC-BBMS-02：双通道隔离
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-BBMS-02 |
| 级别 | 单元测试 |
| 前置 | 分别构建 HMI 和 BBMS 的 state，各自调用 `build_periodic_tx_frames` |
| 步骤 | ① 两个通道各自发送 3 轮 ② 比较 frameId 序列 |
| 预期 | 两通道 frameId 各自独立递增，不共享计数器 |
| 现有测试 | [✓] `test_build_matrix_messages_instances_are_independent` |

---

## 4. 周期上送测试（FR-TX）

### TC-TX-01：payload 长度
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-TX-02 |
| 级别 | 单元测试 |
| 前置 | 每个报文 profile 定义 |
| 步骤 | 构建帧 → 检查 payload 长度 |
| 预期 | SumInfo=310B, Fault=25B, Volt=1012B, Temp=1188B, CellBalSt=52B, CellSdr=416B |
| 现有测试 | [✓] `test_fault_payload_length`, `test_volt_payload_length` 等 6 项 |

### TC-TX-02：周期精度
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-TX-01a~f |
| 级别 | 集成测试 |
| 前置 | 模拟器连接 HMI，抓包 35s |
| 步骤 | 统计每种报文首字节到达时间戳 |
| 预期 | SumInfo/Fault/Volt/Temp 间隔 1000±100ms, CellBalSt 10000±500ms, CellSdr 30000±1000ms |
| 现有测试 | [✗] |
| 备注 | 需要时间敏感测试，可用 `time.monotonic` 在 mock 中验证 |

### TC-TX-03：frameId 递增
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-TX-03 |
| 级别 | 单元测试 |
| 前置 | 连续调用 `build_periodic_tx_frames` 3 次 |
| 步骤 | 检查每次返回帧的 frameId |
| 预期 | frameId 逐次 +1，255 后回到 0 |
| 现有测试 | [✓] `test_str_ctrl_hb_increments_each_suminfo`（间接验证） |

### TC-TX-04：分频正确性
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-TX-04 |
| 级别 | 单元测试 |
| 前置 | scheduler_tick=0 |
| 步骤 | 调用 `_should_send_message('cellbalst', tick, 1.0)` tick 从 0→20 |
| 预期 | tick=0,10,20 返回 True；其余 False |
| 现有测试 | [✓] `test_cellbalst_sends_every_10_base_ticks` |

### TC-TX-05：可配置报文集合
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-TX-05 |
| 级别 | 单元测试 |
| 前置 | 模拟器配置只含 `suminfo,fault` |
| 步骤 | 构建一轮 Tx |
| 预期 | 仅 SumInfo 和 Fault 两帧，无 Volt/Temp/CellBalSt/CellSdr |
| 现有测试 | [✓] `test_configurable_message_set` |

---

## 5. 报文内容测试（FR-MSG）

### TC-MSG-01：StrCtrlHb 自增
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-01 |
| 级别 | 单元测试 |
| 前置 | state.str_ctrl_hb=0 |
| 步骤 | 连续调用 `build_suminfo_payload` 3 次 |
| 预期 | payload[159:161] 小端读出的值 = 1, 2, 3 |
| 现有测试 | [✓] `test_str_ctrl_hb_increments_each_suminfo`, `test_str_ctrl_hb_increments_by_one_not_256` |

### TC-MSG-02：RBMS_St 初始值
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-02 |
| 级别 | 单元测试 |
| 前置 | 默认 CSV 加载 |
| 步骤 | 构建 SumInfo payload，检查 byte 0 |
| 预期 | `payload[0] & 0x01 == 1`（Enable）, `payload[0] & 0x02 == 2`（Online） |
| 现有测试 | [✓] `test_suminfo_initial_status_bits` |

### TC-MSG-03：物理值→raw 编码
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-03 |
| 级别 | 单元测试 |
| 前置 | CSV 中某信号定义 `value=3.65, resolution=0.001, offset=0` |
| 步骤 | 加载 CSV，构建 payload，找到该信号位置 |
| 预期 | raw = round((3.65-0)/0.001) = 3650 |
| 现有测试 | [✓] `test_build_from_csv_respects_resolution` |

### TC-MSG-04：Animate 缓变
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-04 |
| 级别 | 单元测试 |
| 前置 | CSV 标记 `animate=true`，`value=3.5, amplitude=0.5` |
| 步骤 | tick=0, 1, 2 时分别构建 payload |
| 预期 | 信号值按正弦变化，tick 不同值不同，且在 `[3.0, 4.0]` 范围内 |
| 现有测试 | [✓] `test_animate_changes_volt_cell` |

### TC-MSG-05：Animate=false 不变
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-05 |
| 级别 | 单元测试 |
| 前置 | CSV 标记 `animate=false` |
| 步骤 | tick=0, 1, 2 时分别构建 payload |
| 预期 | 信号值保持不变 |
| 现有测试 | [✗] |

### TC-MSG-06：Fault 位图
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-MSG-06 |
| 级别 | 单元测试 |
| 前置 | Fault CSV 中某故障位 value=1 |
| 步骤 | 构建 Fault payload |
| 预期 | 对应 bit=1 |
| 现有测试 | [✗] |

---

## 6. 下行命令测试（FR-RX）

### TC-RX-01：CtlWord 解析与应答
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RX-01, FR-RX-01a |
| 级别 | 单元测试 |
| 前置 | 构造 transportType=0x02 的 BBMS_CtlWord 帧，payload 7B |
| 步骤 | 调用 `dispatch()` |
| 预期 | ① state.bbms_ctrl 被更新 ② 返回列表含 1 帧 ③ 回复帧 transportType=0x03 ④ payload=[0] ⑤ frameId 匹配 |
| 现有测试 | [✓] `test_bbms_ctl_word_reply_is_one_byte` |

### TC-RX-02：CtlWord 不自动应答
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RX-01b, FR-RX-04 |
| 级别 | 单元测试 |
| 前置 | 同 TC-RX-01，但 `auto_reply=False` |
| 步骤 | 调用 `dispatch(auto_reply=False)` |
| 预期 | 返回空列表 |
| 现有测试 | [✓] `test_bbms_ctl_word_no_reply_when_disabled` |

### TC-RX-03：SafetySignal 解析
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RX-02 |
| 级别 | 单元测试 |
| 前置 | 构造 BBMS_SafetySignal 帧（0x02:0x0E），payload=[0x01, 0x05, 0xAA] |
| 步骤 | 调用 `dispatch()` |
| 预期 | ① state.bbms_safety.container_epo_flg=1 ② rolling_counter=5 ③ checksum=0xAA ④ 返回空列表 |
| 现有测试 | [✓] `test_bbms_safety_signal_updates_state` |

### TC-RX-04：未知命令
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RX-03 |
| 级别 | 单元测试 |
| 前置 | 构造未知 cmdGroup/cmdId 的帧 |
| 步骤 | 调用 `dispatch()` |
| 预期 | 返回空列表，不抛异常 |
| 现有测试 | [✗] |

### TC-RX-05：CtlWord 短 payload 保护
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RX-01（异常路径） |
| 级别 | 单元测试 |
| 前置 | payload 长度 < 7 |
| 步骤 | 调用 `dispatch()` |
| 预期 | 日志 `WARNING` 提示 payload 过短，返回空列表 |
| 现有测试 | [✗] |

---

## 7. 配置测试（FR-CFG）

### TC-CFG-01：TOML 默认加载
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CFG-01~06 |
| 级别 | 单元测试 |
| 前置 | 使用默认 `config/rbms_sim.toml` |
| 步骤 | 调用 `load_sim_config()` |
| 预期 | 返回 `SimConfig` 对象，各字段类型正确、值符合默认 |
| 现有测试 | [✓] `test_load_sim_config_hmi_client_defaults` |

### TC-CFG-02：配置覆盖
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CFG-02~06 |
| 级别 | 单元测试 |
| 前置 | 自定义 TOML 内容 |
| 步骤 | 写入临时文件 → `load_sim_config()` |
| 预期 | 各字段取自定义值 |
| 现有测试 | [✓] `test_load_sim_config_bbms_and_hmi_overrides` |

### TC-CFG-03：CLI 参数覆盖
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CFG-07 |
| 级别 | 集成测试 |
| 前置 | 启动模拟器 `--interval 2.0 --no-reply` |
| 步骤 | 抓包或行为验证 |
| 预期 | 上送间隔 2s，CtlWord 无应答 |
| 现有测试 | [✗] |

### TC-CFG-04：--init-config
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CFG-08 |
| 级别 | 集成测试 |
| 前置 | 空目录 |
| 步骤 | `rbms-sim --init-config --config /tmp/test.toml` |
| 预期 | 文件 `/tmp/test.toml` 生成，内容合规 TOML |
| 现有测试 | [✗] |

### TC-CFG-05：--init-matrix-config
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CFG-09 |
| 级别 | 集成测试 |
| 前置 | 空目录 |
| 步骤 | `rbms-sim --init-matrix-config` |
| 预期 | 6 个 CSV 文件生成在 `config/` 下 |
| 现有测试 | [✓] `test_write_default_suminfo_csv_copies_template` |

---

## 8. CSV 数据源测试（FR-CSV）

### TC-CSV-01：CSV 列定义
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CSV-02 |
| 级别 | 单元测试 |
| 前置 | 读取默认 SumInfo CSV |
| 步骤 | 用 `load_matrix_csv()` 解析 |
| 预期 | 返回的 `MatrixCsvSettings` 中 signals 每项含 signal/byteOffset/bitOffset/bitLength/dataType/resolution/offset/unit/value/animate/amplitude |
| 现有测试 | [✓] `test_default_suminfo_signals_matches_template` |

### TC-CSV-02：CSV 热加载
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CSV-03 |
| 级别 | 集成测试 |
| 前置 | 模拟器运行中，CSV 文件值 V1 |
| 步骤 | ① 抓取当前 payload ② 修改 CSV 某 value 为 V2 ③ 等 1 周期 |
| 预期 | payload 中对应字段变为 V2 的编码值 |
| 现有测试 | [✓] `test_reload_suminfo_csv_skips_unchanged_mtime` |

### TC-CSV-03：StrCtrlHb 不被 CSV 覆盖
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-CSV-04 |
| 级别 | 单元测试 |
| 前置 | CSV 中 RBMS_StrCtrlHb value=999 |
| 步骤 | 构建 SumInfo payload |
| 预期 | payload[159:161] != 999，而是当前心跳值 |
| 现有测试 | [✓] `test_build_suminfo_overrides_str_ctrl_hb_not_csv_value` |

---

## 9. 运行时测试（FR-RUNTIME）

### TC-RUNTIME-01：日志分级
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RUNTIME-01 |
| 级别 | 集成测试 |
| 前置 | 启动模拟器 |
| 步骤 | ① 默认日志级别 ② `-v` 模式 |
| 预期 | 默认看得到 `已连接`/`Server 监听`/`BBMS_CtlWord`；`-v` 下更多 DEBUG 帧细节 |
| 现有测试 | [✗] |

### TC-RUNTIME-02：无外部依赖
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RUNTIME-02 |
| 级别 | 静态检查 |
| 步骤 | `grep -r 'import.*sqlite3\|import.*http\|import.*flask\|import.*django' src/` |
| 预期 | 无外部数据库或 Web 框架依赖 |
| 现有测试 | [—] |

### TC-RUNTIME-03：优雅退出
| 项 | 内容 |
| :--- | :--- |
| 需求 | FR-RUNTIME-03 |
| 级别 | 集成测试 |
| 前置 | 模拟器运行中 |
| 步骤 | 发送 SIGINT（Ctrl+C） |
| 预期 | ① 日志 `收到中断，退出` ② 进程退出码 0 ③ 所有 socket 关闭（无 TIME_WAIT 残留） |
| 现有测试 | [✗] |

---

## 10. 非功能测试（NFR）

### TC-NFR-01：代码静态检查
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-02 |
| 级别 | 自动门禁 |
| 命令 | `cd bmsSim/rbms_tcp_sim && uv run ruff check src tests && uv run ty check src tests` |
| 预期 | 无错误 |
| 现有测试 | [✓] `ruff check` + `ty check` 在 CI 中 |

### TC-NFR-02：测试覆盖率
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-03 |
| 级别 | 度量 |
| 命令 | `cd bmsSim/rbms_tcp_sim && uv run pytest --cov=src --cov-report=term-missing` |
| 预期 | 核心模块（protocol/handlers/matrix_runtime）≥80% |
| 现有测试 | [✓] 需补充缺失行 |

### TC-NFR-03：CPU 使用率
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-04 |
| 级别 | 集成测试 |
| 前置 | 模拟器运行，无额外连接 |
| 步骤 | `ps -p <pid> -o %cpu` 取样 10s |
| 预期 | < 5% |
| 现有测试 | [—] |

### TC-NFR-04：内存占用
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-05 |
| 级别 | 集成测试 |
| 前置 | 模拟器运行 5 分钟 |
| 步骤 | `ps -p <pid> -o rss` |
| 预期 | < 50MB |
| 现有测试 | [—] |

### TC-NFR-05：热加载延迟
| 项 | 内容 |
| :--- | :--- |
| 需求 | NFR-06 |
| 级别 | 集成测试 |
| 前置 | 模拟器运行中 |
| 步骤 | 修改 CSV mtime → 观察下次 Tx payload |
| 预期 | 修改后 ≤1 个 tick 内生效 |
| 现有测试 | [✓] `test_hot_reload_keeps_str_ctrl_hb_in_signals` |

---

## 11. 验收测试（冒烟）

### TC-SMOKE-01：全量周期 Tx
| 项 | 内容 |
| :--- | :--- |
| 需求 | 验收准则 §6.1, §6.2 |
| 级别 | 冒烟测试 |
| 步骤 | ① 启动 HMI Server（mock） ② 启动 BBMS Client（mock） ③ 启动模拟器 ④ 抓包 35s |
| 预期 | ① HMI 通道收到 6 类报文 ② BBMS 通道收到 6 类报文 ③ 各周期符合规范 ④ 帧格式合规 |
| 现有测试 | [✗] |

### TC-SMOKE-02：CtlWord 收发
| 项 | 内容 |
| :--- | :--- |
| 需求 | 验收准则 §6.2 |
| 级别 | 冒烟测试 |
| 步骤 | ① BBMS Client 发送 CtlWord（transportType=0x02） ② 等待回复 |
| 预期 | 收到回复帧：transportType=0x03, payload=[0x00] |
| 现有测试 | [✓] `test_bbms_server_replies_ctl_word` |

### TC-SMOKE-03：断线重连
| 项 | 内容 |
| :--- | :--- |
| 需求 | 验收准则 §6.2 |
| 级别 | 冒烟测试 |
| 步骤 | ① 模拟器 HMI 已连接 ② HMI Server 断开 ③ 5s 内重启 HMI Server |
| 预期 | 模拟器自动重连，恢复 Tx |
| 现有测试 | [✗] |

---

## 12. 已有测试覆盖率汇总

| 类别 | 总数 | 已有 | 缺失 | 覆盖率 |
| :--- | :--: | :--: | :--: | :----: |
| 通信层 | 8 | 2 | 6 | 25% |
| HMI 通道 | 2 | 1 | 1 | 50% |
| BBMS 通道 | 2 | 2 | 0 | 100% |
| 周期上送 | 5 | 3 | 2 | 60% |
| 报文内容 | 6 | 3 | 3 | 50% |
| 下行命令 | 5 | 3 | 2 | 60% |
| 配置 | 5 | 2 | 3 | 40% |
| CSV 数据源 | 3 | 3 | 0 | 100% |
| 运行时 | 3 | 0 | 3 | 0% |
| 非功能 | 5 | 2 | 3 | 40% |
| 冒烟 | 3 | 1 | 2 | 33% |
| **合计** | **47** | **22** | **25** | **47%** |

---

## 13. 优先补齐的测试（按风险排序）

| 优先级 | TC ID | 原因 |
| :----: | :---- | :--- |
| P0 | TC-RX-04 | 未知命令不应抛异常，影响健壮性 |
| P0 | TC-RX-05 | CtlWord 短 payload 保护，防崩溃 |
| P0 | TC-TX-02 | 周期精度是核心契约 |
| P1 | TC-COM-01~04 | 基本通信可用性 |
| P1 | TC-MSG-02 | SumInfo 初始状态 |
| P1 | TC-TX-05 | 可配置性验证 |
| P2 | TC-COM-08 | 小端编码正确性 |
| P2 | TC-RUNTIME-03 | 优雅退出 |
