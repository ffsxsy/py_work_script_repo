---
description: C 代码生成规则（版本锚点与质量基线）
globs: "**/*.{c,h,cc,cpp,hpp,cxx,hxx}"
alwaysApply: false
---

# C 代码生成规则

## 0) 本仓库生成物（MUST）

- `McuCanMap_script/c_struct_*.c` 由 `gen_*.py` 生成；**默认改 Python 生成器**，仅在用户明确要求时手改 `.c`。
- 手改生成文件时，须在回复中提醒：下次跑生成脚本可能被覆盖。

## 1) 版本与构建（MUST）

- 生成代码前先读取（若存在）：`CMakeLists.txt`、`compile_commands.json`、`README`、`docs/`。
- 必须先确定 `C` 标准；优先项目声明，缺失时明确写出假定（如 `C11/C17`）。
- 未确认标准与编译器行为前，不输出实现代码。
- 构建系统优先 `CMakeLists.txt`；尽量使用 CMake 代替 Makefile。

## 2) 接口与类型（MUST）

- 使用清晰、可移植类型（如 `size_t`、`uint32_t`），避免依赖实现细节。
- 公共接口必须说明输入/输出约束与错误码语义。
- 复杂函数必须给出函数级说明（注释或文档块），至少包含：入参约束、出参/副作用、返回值语义、错误码或失败条件。

## 3) 安全与可靠性（MUST）

- 优先当前版本推荐 API，禁止已废弃 API。
- 示例代码必须完整可运行，包含必要错误处理与边界处理。
- 系统调用与资源申请必须检查返回值（`malloc/calloc/realloc/open/read/write/...`）。
- 禁用不安全字符串 API：`gets`、`strcpy`、`sprintf`、`strcat`；优先 `snprintf/strncpy/strncat`。
- 资源必须成对释放（`malloc/free`、`open/close`、`lock/unlock`）。
- 安全基线对齐 **CERT C / MISRA C**：避免未定义行为，执行显式边界检查与错误处理，保证资源生命周期可追踪。
- 指针、数组、整数转换、并发/中断上下文优先采用 CERT C / MISRA C 推荐写法。

## 4) 嵌入式约束（SHOULD）

- 控制 RAM/Flash 占用与 CPU 开销，避免无界循环和不受控内存增长。
- 避免频繁动态分配与深递归；优先静态分配、对象池、定长缓冲区、环形缓冲区。
- 中断和实时路径保持短小确定，避免阻塞调用与不可预测延迟。
- 接口应明确时序、超时、失败回退与降级策略。

## 5) CI 与测试（MUST）

- 推荐工具：`clang-format`、`clang-tidy`、`Cppcheck`、`CTest`。
- CI 基线命令（新增/修改后应优先通过）：`clang-format --dry-run`、`clang-tidy`、`cppcheck`、`ctest`。
- 结果必须给出命令执行顺序，便于直接接入 CI。
- 测试质量至少覆盖：边界值、异常路径、回归场景；关键模块需有失败分支断言。
- 若项目缺少既有命令，先说明缺失，再给最小可落地命令。

## 6) 固定输出模板（MUST）

- 输出顺序：版本依据 -> 运行环境 -> 依赖/构建步骤 -> 完整代码 -> 运行命令 -> 校验命令 -> 风险与限制。
- 运行环境必须写明：语言标准、编译器版本（若可得）、关键库版本。

## 7) Definition of Done（DoD）

- [ ] 已明确 C 标准与版本依据（或显式假定）。
- [ ] 已给出运行/校验命令与执行顺序（可直接接入 CI）。
- [ ] 已说明测试覆盖边界值、异常路径、回归场景（关键模块含失败断言）。

## 8) 变更日志（Changelog）

- 2026-05-25：增加生成物 `c_struct_*.c` 优先改生成器说明。
- 2026-04-23：新增 DoD 与 Changelog 区块，统一 C 规则验收与维护方式。
