---
description: 技术 Markdown 速读规则（短版，Obsidian 优先；仅 docs 下笔记）
globs: "docs/**/*.md"
alwaysApply: false
---

# 技术 Markdown 规则（短版）

## 1. Scope

- 本规则只约束 Markdown/Obsidian 的写法与结构。
- 代码可运行性、版本、安全、测试统一遵循 `zh-engineering-standards.mdc`。
- 详细解释与示例见 `technical-obsidian-markdown-examples.mdc`。

## 2. MUST

- 回答与文档默认中文，先结论后解释，结构化表达。
- 全文仅一个 `#`，标题层级不跳级（`# -> ## -> ### -> ####`）。
- 长文（多章节）在 `#` 后提供目录：`## 0. 目录` 或 `## Table of Contents`。
- 新建或大改前先检索同主题内容，优先写增量并使用 `[[链接]]` 或 `![[嵌入]]` 复用。
- 围栏代码块必须标注语言（如 `python`、`bash`、`yaml`、`mermaid`、`diff`）。
- 可执行代码块必须遵守版本锚定与安全要求；仅示意代码必须标注“示意 / 非完整可运行”。
- 复杂函数说明至少包含：入参约束、出参/副作用、返回值语义、失败条件。
- API/配置/环境变量说明必须使用表格（参数名、类型、默认值、必填、描述）。

## 3. SHOULD

- 用户未指定 GFM 时，优先 Obsidian 语法（`[[ ]]`、`![[ ]]`、callout、frontmatter、tags）。
- 单篇建议控制在约 `<=600` 行，超长内容拆分并互链。
- 复杂流程优先 `mermaid`，结构化数据优先表格。
- 中英文、数字、链接之间保留空格；术语与命名全篇一致。

## 4. MUST NOT

- 不得把伪代码当可运行示例输出。
- 不得静默复制已知错误/过时旧文内容。
- 不得在示例中泄露密钥、密码、令牌等敏感信息。
- 不得在同一主题下重复堆叠近似内容而不建立互链。

## 5. Output Template

- 推荐输出顺序：目标与范围 -> 前置条件/版本依据 -> 步骤与示例 -> 校验方式 -> 风险与边界 -> 相关链接。

## 6. Checklist

- [ ] 已检索同类主题并做去重互链。
- [ ] 标题层级合法（单一 H1、无跳级）。
- [ ] 代码块有语言标签；示意代码有明确标注。
- [ ] API/配置项已表格化，流程图/表格与正文一致。
- [ ] 技术结论可核对，且无敏感信息泄露。
